import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  square: {
    width: 12,
    height: 12,
  },
  legendText: {
    color: 'white',
  },
  individualLegendText: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default style;
